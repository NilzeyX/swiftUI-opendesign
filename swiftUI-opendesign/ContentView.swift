import SwiftUI

struct ContentView: View {
        
    var body: some View {
        NavigationStack {
            LaunchView()
        }
    }
}

#Preview {
    ContentView()
        .preferredColorScheme(.dark)
}
